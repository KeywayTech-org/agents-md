# Agent Rules

This `agents-md` project maintains reusable instructions for AI coding agents.

- `AGENTS.md` is the canonical rule set.
- `%USERPROFILE%\.codex\AGENTS.md`, `%USERPROFILE%\.trae-cn\AGENTS.md`, and `%USERPROFILE%\.workbuddy\AGENTS.md` are synchronized local copies for their respective tools.
- Update the canonical file first, then refresh all local copies to keep them identical.
- Keep the local copies identical to the canonical file.
