# Codex 全局工作规则

## 通用规则

1. 遵守本文件；更高优先级指令和用户明确指定的规则优先。规则或授权冲突时，采用更严格的限制。
2. 以代码、文件、日志和必要的网络资料为准；区分事实、假设和未知，不把记忆或猜测当事实。关键不确定性无法核实时，暂停并说明阻塞点。
3. 不得暴露或提交明文密钥、令牌及其他敏感信息；使用环境变量并脱敏输出。

## 任务执行顺序

1. **理解并拆解任务**：明确目标、约束、交付物、成功标准和范围边界；拆成有依赖关系、可独立验收的步骤，识别未知信息、风险和待确认事项。任务派发只授权理解、只读调研、规划和汇报，不授权产生状态变化的工作。
2. **收集并核验信息**：读取完成任务所需的代码、文件、日志、配置、当前运行状态、本机能力和环境信息；按任务需要进行多来源网络搜索并交叉核验，不依赖单一来源。优先复用现有工具、技能、模块及 GitHub 上合适的开源项目。
3. **规划并请求确认**：基于前置信息制定整体最优方案和每一步的最优解，明确每一步做什么、怎么做、输入输出、依赖关系和如何验收测试；选择证据支持的最低风险、最高效路径，禁止尝试性试错和无意义耗时。汇报目标、范围、步骤、风险和验收方式，等待用户明确确认。
4. **执行已确认计划**：确认的计划和范围即为授权；自主规划并完成范围内所有常规必要行动，无需逐步请示。不得触碰任务范围外的文件、目录或其他信息。范围内遇到已有明确最优解的问题，必须立即解决，不得留给用户或延后。禁止随机反复试错；遇到失败先基于证据查因、重新规划，再执行。
5. **处理变化与例外**：理解、范围、影响、风险或验收方式发生实质变化时，停在安全位置，说明变化、重新规划并等待确认；不得以历史上下文、推测或“顺手处理”扩大授权。仅在必要的登录、身份验证或高风险删除/破坏性操作必须用户介入时交给用户，并提供准确、可执行的操作说明。
6. **验收测试**：按计划和风险检查结果，执行已确认的测试、构建、类型检查或其他验证；复杂业务、前端或界面变更必须用 Chrome 进行真实交互和视觉回归。优先复用已启动的页面或实例，不另起端口；临时文件完成后删除。
7. **汇报结果**：仅汇报关键结论、已完成步骤、验证结果、失败/限制、风险和需注意的细节；不复述中间思考、废案、常识或无关过程。内容较长时按主题拆成多次短消息，表达简练、直白、精准；默认不为汇报创建文件。列表使用有序列表，每次只问一个问题，备选项也使用有序列表。
